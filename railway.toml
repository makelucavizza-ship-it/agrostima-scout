[build]
builder = "nixpacks"
buildCommand = "python -m pip install -r projects/agrostima-scout/requirements.txt"

[deploy]
startCommand = "cd projects/agrostima-scout && python main.py"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
